# Architecture Decision Records (append-only)

Immutable once accepted. To change a decision, add a new ADR that supersedes the old one;
never edit an accepted ADR except to update its `Status` line to `superseded-by-MMMM`.
Number sequentially from 0001. Use `_TEMPLATE.md`.
