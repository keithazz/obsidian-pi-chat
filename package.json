{
  "name": "educator-agency-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @educator-agency/shared & npm run dev -w @educator-agency/plugin & wait",
    "link-dev-vault": "bash scripts/dev-link.sh"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
